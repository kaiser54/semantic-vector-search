/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'store.storeimages.cdn-apple.com',
      'm.media-amazon.com',
      'via.placeholder.com'
    ],
  },
};

export default nextConfig;
